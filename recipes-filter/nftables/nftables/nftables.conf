table inet filter {
    chain prerouting_devices {
        type filter hook prerouting priority 0;
        meta l4proto tcp jump LOG_DeviceBlocked_DROP;
    }

    chain LOG_DeviceBlocked_DROP {
        counter
        log prefix "LOG_DeviceBlocked_DROP: " limit rate 1/minute, drop
    }
}
